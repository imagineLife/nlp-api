describe('POST nlp/sentiment', () => {
  let sentences = 'This is the first sentence. The second sentence is here. Third sentence will be the last.'

  it('works', () => {
    expect(2).toBe(2)
  })
})