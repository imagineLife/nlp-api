// import setupTestDb from './setupTestDb.js'
export default async function jestGlobalSetup(globalConfig, projectConfig) {
  // let { MongoClient} = await setupTestDb()
  console.log('jest global setup done')
}